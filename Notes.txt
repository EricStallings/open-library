Flow For Redux: 

Action Types - define the action type
THEN
actions.js - define the action creator 
THEN 
reducer.js - adds the SWITCH case for the action type 

Next Notes: 
Build Sync actions:
1. Update SS (handleSearchBarUpdate) CHECK
2. Update Book List CHECK
  > Should change isFetching to False (in addition to updating the book list) 
3. Start Fetching action which changes the isFetching property to TRUE CHECK
  > Need an isFetching in the store. 



3. Create asynchronous 

export const fetchWithSS = text => dispatch => {
  dispatch(startFetching);
  // construct URL from text here
  fetch(URL)
  .then(resp => resp.json())
  .then(data => {
    // dispatch updateBL(data or some version of the data. We expect an array.)
  })

}